# 🧪 دليل اختبار المشروع

## نظرة عامة
هذا الدليل يشرح كيفية اختبار جميع ميزات مشروع هميان بشكل كامل.

---

## ✅ قائمة الاختبار الشاملة

### المرحلة 1: إعداد البيئة
- [ ] Node.js مثبت (إصدار 18 أو أحدث)
- [ ] تم تشغيل `npm install`
- [ ] ملف `.env` موجود ومُعد بشكل صحيح
- [ ] السيرفر يعمل بدون أخطاء
- [ ] الرسالة تظهر: `Himyan server listening on 0.0.0.0:3000`

---

### المرحلة 2: اختبار الصفحة الرئيسية

#### الخطوات:
1. افتح: http://localhost:3000
2. تحقق من:
   - [ ] الشعارات تظهر (QCB + هميان)
   - [ ] القائمة تعمل
   - [ ] زر "قدّم طلبك الآن" يعمل
   - [ ] تبديل اللغة يعمل (AR ↔ EN)
   - [ ] جميع الأقسام تظهر بشكل صحيح
   - [ ] الصور والأيقونات تحمّل

#### النتيجة المتوقعة:
✅ صفحة رئيسية كاملة بدون أخطاء

---

### المرحلة 3: اختبار نموذج التقديم

#### الخطوات:
1. اضغط "قدّم طلبك الآن"
2. املأ النموذج:
   - الاسم الكامل: `أحمد محمد علي`
   - رقم الهوية: `12345678901`
   - رقم الجوال: `12345678`
   - البريد: `test@example.com`
   - الجنسية: مواطن
   - نوع البطاقة: خصم مباشر
   - البنك: بنك قطر الوطني
3. اضغط "متابعة"

#### التحقق:
- [ ] جميع الحقول تعمل
- [ ] التحقق من البيانات يعمل
- [ ] الانتقال لصفحة الدفع نجح

#### النتيجة المتوقعة:
✅ الانتقال إلى صفحة الدفع (`confirmation.html`)

---

### المرحلة 4: اختبار بوابة الدفع الوهمية

#### الخطوات:
1. في صفحة الدفع، أدخل:
   - رقم البطاقة: `4242424242424242`
   - التاريخ: `12/25`
   - CVV: `123`
   - الاسم: `Ahmed Ali`
2. اضغط "ادفع 100 ريال"

#### التحقق:
- [ ] جميع الحقول تقبل الإدخال
- [ ] التحقق من صيغة البطاقة يعمل
- [ ] الانتقال لصفحة الانتظار نجح

#### النتيجة المتوقعة:
✅ الانتقال إلى `pending.html`
✅ رسالة "طلبك قيد المراجعة"

---

### المرحلة 5: اختبار لوحة الأدمن

#### الخطوات:
1. افتح تبويب جديد: http://localhost:3000/admin.html
2. سجّل دخول:
   - اسم المستخدم: `admin`
   - كلمة المرور: `HimyanAdmin2026!Qcb#`

#### التحقق:
- [ ] تسجيل الدخول نجح
- [ ] لوحة التحكم تظهر
- [ ] الطلب الجديد يظهر في الجدول
- [ ] الحالة: "جديد"
- [ ] الزوار النشطون يظهر (1 على الأقل)

#### النتيجة المتوقعة:
✅ لوحة أدمن تعمل بالكامل

---

### المرحلة 6: اختبار قبول/رفض الطلب

#### الخطوات:
1. في لوحة الأدمن، ابحث عن الطلب
2. اضغط "قبول"
3. تحقق من تغيير الحالة إلى "مقبول"

#### التحقق:
- [ ] زر "قبول" يعمل
- [ ] الحالة تتحدث تلقائيًا
- [ ] لون الحالة يتغير

#### النتيجة المتوقعة:
✅ الحالة تتحول إلى "مقبول — أدخل الرمز"

---

### المرحلة 7: اختبار رمز التأكيد (6 أرقام)

#### الخطوات:
1. ارجع لتبويب المستخدم
2. إذا كنت في `pending.html`، حدّث الصفحة
3. يجب أن تنتقل تلقائيًا إلى `confirmation-code.html`
4. أدخل رمز من 6 أرقام: `123456`
5. اضغط "تأكيد"

#### التحقق:
- [ ] الصفحة تظهر بشكل صحيح
- [ ] حقل الإدخال يقبل 6 أرقام فقط
- [ ] الأرقام العربية تُحول تلقائيًا للإنجليزية
- [ ] الانتقال نجح

#### النتيجة المتوقعة:
✅ الانتقال إلى `atm-pin.html` 🆕

---

### المرحلة 8: اختبار رقم ATM (4 أرقام) 🆕

#### الخطوات:
1. أنت الآن في `atm-pin.html`
2. أدخل رقم ATM من 4 أرقام: `1234`
3. اضغط "التالي"

#### التحقق:
- [ ] الصفحة تظهر بشكل صحيح
- [ ] حقل الإدخال يقبل 4 أرقام فقط
- [ ] الرقم مخفي (type="password")
- [ ] الأرقام العربية تُحول للإنجليزية
- [ ] الانتقال نجح

#### النتيجة المتوقعة:
✅ الانتقال إلى `network-provider.html` 🆕

---

### المرحلة 9: اختبار اختيار مزود الشبكة 🆕

#### الخطوات:
1. أنت الآن في `network-provider.html`
2. اختر أحد المزودين:
   - فودافون قطر
   - أوريدو قطر
3. اضغط "متابعة"

#### التحقق:
- [ ] الصفحة تظهر بشكل صحيح
- [ ] البطاقتان تظهران (فودافون + أوريدو)
- [ ] يمكن اختيار واحدة فقط
- [ ] البطاقة المختارة تتمايز بصريًا
- [ ] زر "متابعة" يُفعّل بعد الاختيار
- [ ] الانتقال نجح

#### النتيجة المتوقعة:
✅ إذا اخترت فودافون → `vodafone-login.html` 🆕  
✅ إذا اخترت أوريدو → `ooredoo-login.html` 🆕

---

### المرحلة 10: اختبار تسجيل دخول المزود 🆕

#### الخطوات:
1. أنت الآن في صفحة تسجيل الدخول
2. أدخل:
   - رقم الجوال: `+97412345678`
   - كلمة المرور: `password123`
3. اضغط "تسجيل الدخول"

#### التحقق:
- [ ] الصفحة تظهر بشكل صحيح
- [ ] شعار المزود يظهر
- [ ] الحقلان يعملان
- [ ] كلمة المرور مخفية
- [ ] الانتقال نجح

#### النتيجة المتوقعة:
✅ الانتقال إلى `pending.html?stage=complete` 🆕
✅ رسالة "طلبك مكتمل"

---

### المرحلة 11: التحقق من البيانات في لوحة الأدمن 🆕

#### الخطوات:
1. ارجع لتبويب الأدمن
2. حدّث الصفحة أو اضغط "تحديث"
3. ابحث عن الطلب

#### التحقق:
- [ ] الحالة تحدثت إلى "مكتمل"
- [ ] عمود "رقم ATM" يظهر زر "عرض"
- [ ] عمود "مزود الشبكة" يظهر المزود المختار
- [ ] اضغط "التفاصيل"

#### النتيجة المتوقعة:
✅ نافذة التفاصيل تفتح
✅ جميع البيانات تظهر

---

### المرحلة 12: اختبار فك التشفير 🆕

#### الخطوات:
1. في نافذة التفاصيل
2. بجانب "رقم ATM" اضغط زر "عرض"
3. بجانب "كلمة مرور المزود" اضغط زر "عرض"

#### التحقق:
- [ ] نافذة منبثقة تظهر برقم ATM: `1234`
- [ ] نافذة منبثقة تظهر بكلمة المرور: `password123`

#### النتيجة المتوقعة:
✅ فك التشفير يعمل بنجاح!

---

## 🔍 اختبارات إضافية

### اختبار 1: التحقق من التشفير في قاعدة البيانات

#### إذا كنت تستخدم PostgreSQL:
```sql
SELECT atm_pin_encrypted, provider_password_encrypted 
FROM requests 
WHERE id = 'request-id-here';
```

#### إذا كنت تستخدم JSON:
افتح `data/requests.json` وتحقق:
```json
{
  "atmPinEncrypted": "iv:encrypted_data",
  "providerPasswordEncrypted": "iv:encrypted_data"
}
```

#### التحقق:
- [ ] القيم مُشفرة (ليست نصًا واضحًا)
- [ ] تحتوي على `:` (فاصل بين IV والبيانات)

---

### اختبار 2: تبديل اللغة

#### الخطوات:
1. في أي صفحة، اضغط زر "EN" أو "AR"
2. تحقق من تغيير اللغة

#### التحقق:
- [ ] جميع النصوص تتغير
- [ ] اتجاه الصفحة يتغير (RTL ↔ LTR)
- [ ] جميع الصفحات الجديدة تدعم اللغتين

---

### اختبار 3: التحقق من الأخطاء

#### الخطوات:
1. حاول إدخال رمز تأكيد أقل من 6 أرقام
2. حاول إدخال رقم ATM أقل من 4 أرقام
3. حاول إرسال نموذج فارغ

#### النتيجة المتوقعة:
✅ رسائل خطأ واضحة تظهر
✅ لا تنتقل للصفحة التالية

---

## 📊 جدول نتائج الاختبار

| الرقم | الاختبار | النتيجة | ملاحظات |
|------|----------|---------|---------|
| 1 | إعداد البيئة | ☐ نجح ☐ فشل | |
| 2 | الصفحة الرئيسية | ☐ نجح ☐ فشل | |
| 3 | نموذج التقديم | ☐ نجح ☐ فشل | |
| 4 | بوابة الدفع | ☐ نجح ☐ فشل | |
| 5 | لوحة الأدمن | ☐ نجح ☐ فشل | |
| 6 | قبول الطلب | ☐ نجح ☐ فشل | |
| 7 | رمز التأكيد | ☐ نجح ☐ فشل | |
| 8 | رقم ATM 🆕 | ☐ نجح ☐ فشل | |
| 9 | اختيار المزود 🆕 | ☐ نجح ☐ فشل | |
| 10 | تسجيل دخول 🆕 | ☐ نجح ☐ فشل | |
| 11 | عرض البيانات 🆕 | ☐ نجح ☐ فشل | |
| 12 | فك التشفير 🆕 | ☐ نجح ☐ فشل | |

---

## 🐛 إذا واجهت مشاكل

### المشكلة: الصفحة لا تنتقل
**الحل:**
1. افتح Console في المتصفح (F12)
2. تحقق من وجود أخطاء JavaScript
3. تحقق من أن APIs تعمل

---

### المشكلة: البيانات لا تظهر في الأدمن
**الحل:**
1. حدّث الصفحة
2. اضغط زر "تحديث"
3. تحقق من أن السيرفر يعمل

---

### المشكلة: فك التشفير لا يعمل
**الحل:**
1. تحقق من أن `ENCRYPTION_KEY` موجود في `.env`
2. إذا غيّرت المفتاح، البيانات القديمة لن تعمل
3. ابدأ طلب جديد

---

### المشكلة: أرقام ATM تظهر بوضوح
**الحل:**
⚠️ **خطأ خطير!** يجب أن تكون مُشفرة
1. تحقق من `server.js` → دالة `encrypt()`
2. تحقق من قاعدة البيانات

---

## ✅ معايير النجاح

لكي تعتبر المشروع جاهز، يجب أن تنجح في:
- [x] جميع الـ 12 اختبار الأساسية
- [x] الاختبارات الإضافية الـ 3
- [x] لا توجد أخطاء في Console
- [x] لا توجد أخطاء في السيرفر
- [x] التشفير يعمل بشكل صحيح
- [x] جميع الصفحات تظهر بشكل صحيح

---

## 📝 تقرير الاختبار

بعد الانتهاء، اكتب تقرير مختصر:

```
تاريخ الاختبار: __/__/____
المُختبِر: ___________

النتائج:
✅ نجح: __/12 اختبار
❌ فشل: __/12 اختبار

المشاكل المكتشفة:
1. _________________
2. _________________

التوصيات:
1. _________________
2. _________________

الحالة النهائية: ☐ جاهز ☐ يحتاج تحسينات
```

---

## 🎯 الخطوة التالية

بعد اجتياز جميع الاختبارات:
1. ✅ المشروع جاهز للعرض
2. ⚠️ ليس جاهز للإنتاج (يحتاج مراجعة أمنية)
3. 📝 جهّز عرض تقديمي للمدير
4. 📊 راجع `CHANGES_SUMMARY.md` للملخص

---

**حظًا موفقًا في الاختبار! 🚀**
